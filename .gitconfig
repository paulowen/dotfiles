[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
    name = Paul Owen
    email = owen.paul@gmail.com
[credential]
    helper = osxkeychain
[core]
    excludesfile = ~/.gitignore
